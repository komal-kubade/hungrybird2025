# MongoDB connection string
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/forum-app?retryWrites=true&w=majority

# JWT settings
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=7d

# CORS origin
CORS_ORIGIN=http://localhost:3000

# Server
PORT=5000
NODE_ENV=development
